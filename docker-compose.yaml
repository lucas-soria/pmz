version: "3"
services:
    PMZ:
        image: "pmz"
        volumes:
            - "./Logs:/home/lucas/Documents/compu\ 2/pmz/Logs"
        environment: 
            - IP
            - PORT
        ports:
            - $PORT:$PORT
